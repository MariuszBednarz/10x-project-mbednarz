---
import Layout from "@/layouts/Layout.astro";
import { HospitalsListProtected } from "@/components/wards/HospitalsListProtected";

interface Props {
  wardName: string;
}

const { wardName } = Astro.params;

if (!wardName) {
  return Astro.redirect("/wards");
}

const decodedWardName = decodeURIComponent(wardName);
---

<Layout title={`Szpitale - ${decodedWardName}`}>
  <HospitalsListProtected wardName={decodedWardName} client:load />
</Layout>
