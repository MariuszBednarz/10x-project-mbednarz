/**
 * Setup file for Node.js environment tests
 * Used for utility functions, schemas, and services that don't need DOM
 */

// No DOM-specific mocks needed for Node.js environment
// Tests run faster without jsdom overhead
